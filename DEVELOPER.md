# 小红书文案拆解与创作辅助工具

## 文档定位

这份文档用于记录当前阶段的产品规划、技术方案、开发边界和阶段性取舍。

目标不是一次写到“完美”，而是先明确一个适合 0 经验团队落地的 V1 版本，避免一开始把功能做得太重、太散、太难维护。

---

## 1. 产品一句话

通过浏览器插件采集并标注小红书优质笔记，在网页端借助 AI 完成文案拆解、风格学习、初稿生成、逐段改写和去 AI 味优化。

---

## 2. 目标用户

当前优先服务以下人群：

- 需要持续写小红书文案的个人创作者
- 需要参考大量爆文并快速产出草稿的运营人员
- 有明确个人偏好，希望 AI 越写越“像自己”的用户

当前阶段不优先考虑：

- 多团队协作场景
- 企业级权限管理
- 多平台同步创作体系

---

## 3. 产品目标

V1 要解决的核心问题只有 4 个：

1. 把用户喜欢的参考笔记收集起来，并且不是“只收藏不理解”。
2. 让 AI 知道用户到底喜欢哪些片段、为什么喜欢。
3. 让 AI 能基于参考库和用户偏好，给出更像“小红书真人文案”的初稿。
4. 让用户能和 AI 一起逐段修改，而不是一次性吐出一篇大段废话。

---

## 4. 产品边界

### V1 必须做

#### 插件端

- 在小红书笔记页面抓取标题、正文、封面、链接、博主昵称
- 选择已有文件夹或新建文件夹后保存笔记
- 支持“沿用原题”或“手动重命名”
- 支持用户在正文中选中片段
- 对选中片段填写“喜欢/记录理由”
- 为选中片段打颜色标签
- 支持按文件夹查看笔记
- 支持基础搜索、基础筛选、基础排序
- 支持回到原始链接

#### 网页端

- 新建创作项目
- 选择一个参考文案文件夹作为素材来源
- 生成文案分析结果
- 选择篇幅：短 / 中 / 长
- 输入本次创作基础信息
- 生成结构化初稿
- 保留结构色块，支持顺序调整
- 支持用户逐段和 AI 对话改稿
- 支持保存多个版本
- 在最终确认前，提供一次“读者视角”审稿和优化建议

#### AI 能力

- 参考文案文本分析
- 参考文案结构分析
- 基于标注内容理解用户偏好
- 初稿生成
- 去 AI 味二次润色
- 最终审稿

### V1 先不做

这些需求不是没价值，而是当前阶段先不做更稳：

- 自动训练专属模型
- 用户无感“自主学习”
- 多人协作和团队空间
- 复杂权限体系
- 手机版网页深度适配
- 1:1 还原小红书原生浏览界面
- 批量复制粘贴文件
- 文件夹收藏
- 回收站 30 天恢复
- 超复杂筛选器
- 公开发布后的支付系统

### V1.5 可以补做

- 回收站
- 文件夹收藏
- 批量操作
- 更丰富的筛选器
- 封面流式预览视图

### V2 再考虑

- 多人协作
- 团队共享素材库
- 自动归纳用户风格画像
- 训练型个性化模型
- 多平台内容适配

---

## 5. 一个关键判断：不要把“去 AI 味”误解成 Skill 本身

“去 AI 味”不是一个单点功能，而是 3 层能力一起工作：

1. 规则层
2. 记忆层
3. 审稿层

### 规则层

作用：告诉模型“什么叫像人写的，什么叫 AI 味重”。

适合放在：

- prompt 模板
- style rules 文档
- skill/references 文档

### 记忆层

作用：记录用户真实偏好和失败案例。

必须放在：

- 数据库

典型内容：

- 用户收藏并高亮的片段
- 用户填写的喜欢理由
- 用户打的标签
- 用户在改稿时说过“太假”“太像广告”“太端着”的反馈
- 用户最终保留下来的版本

### 审稿层

作用：在草稿生成后，再用一个独立步骤检查“AI 味”。

检查重点：

- 空话多不多
- 套话重不重
- 情绪是不是太平均
- 有没有具体场景
- 像不像真人会发的笔记

### 结论

V1 不做“模型自动学习”，而做“可追踪的反馈记忆 + 规则约束 + 二次审稿”。

这是当前阶段最现实、最稳、最容易做出效果的方案。

---

## 6. Skills 在整个系统中的位置

如果后续要做 Skill，应该把它理解成“AI 工作规则包”，而不是“整个系统的记忆大脑”。

### Skill 适合负责

- AI 分析时的输出格式要求
- 去 AI 味的写作原则
- 风格模仿时的注意事项
- 审稿时的检查清单

### Skill 不适合单独负责

- 长期记住某个用户的偏好
- 记录每次对话中的失败案例
- 存放项目级版本历史

这些都必须落到数据库。

---

## 7. 推荐的 V1 用户流程

1. 用户打开小红书网页笔记
2. 插件抓取笔记基础信息
3. 用户选择保存到哪个文件夹
4. 用户手动高亮喜欢的片段，并填写理由与标签
5. 用户到网页端新建创作项目
6. 选择一个参考文件夹
7. AI 先做分析，再产出结构化初稿
8. 用户选篇幅、补充需求、调整结构
9. 用户逐段和 AI 协作改稿
10. AI 做最终读者视角审稿
11. 用户确认最终版本

---

## 8. 页面与模块建议

### 插件端

建议分成 4 个模块：

#### 1. Popup 面板

用户点插件时看到的主要入口。

包含：

- 当前笔记信息
- 选择文件夹
- 新建文件夹
- 保存按钮
- 文件命名方式

#### 2. Content Script 选区层

负责在小红书页面里进行正文选中、高亮、标注。

包含：

- 用户选中文字
- 弹出小浮层
- 输入理由
- 选择颜色标签
- 保存片段

#### 3. 插件管理页

负责查看文件夹和已存笔记。

V1 只要有：

- 文件夹列表
- 笔记列表
- 搜索
- 基础筛选
- 删除
- 重命名

#### 4. 本地状态与云同步层

负责保存用户登录状态、当前环境信息、临时草稿等。

---

## 9. 网页端模块建议

### 1. 项目列表页

展示用户创建过的创作项目。

### 2. 新建项目页

选择参考文件夹，填写项目主题、目标人群、想要表达的重点。

### 3. 分析页

默认展示“短分析”，而不是长报告。

短分析结构固定为：

1. 一句话判断：直接说这批文案真正值得复用的写法
2. 最有效的 3 个写法：每条都能直接复用
3. 用户偏好：基于用户标注、标签和喜欢理由总结
4. 下次怎么用：给一个明确写作方向

默认字数建议控制在 300-500 字。

分析页的原则：

- 先给结论，不先铺背景
- 结论要前置到句首，不写“这批文案真正值得学的是”这类铺垫
- 不重要的信息往后放；如果不影响用户下一步行动，就删除
- 不逐篇流水账分析
- 不写报告腔、赏析腔、空泛总结
- 用户追问时，再展开用词、结构、读者视角等细节

推荐写法示例：

- 好：先把读者拉进一个具体动作或判断，而不是解释背景。
- 不好：这批文案真正值得学的是：它们不是先解释背景，而是先把读者拉进一个具体动作或判断里。

### 4. 初稿生成页

展示：

- 篇幅选择
- 结构模块
- 初版草稿

### 5. 深度改稿页

左右布局：

- 左侧：当前笔记预览
- 右侧：AI 对话与逐段修改

### 6. 最终审稿页

展示：

- 读者行为模拟
- AI 味风险提示
- 可回退修改入口

---

## 10. 数据结构建议

V1 推荐先按以下核心实体设计。

### users

存用户基本信息。

关键字段：

- id
- nickname
- avatar_url
- created_at

### folders

存参考文案文件夹。

关键字段：

- id
- user_id
- name
- description
- note_count
- created_at

### notes

存每一篇抓取的笔记。

关键字段：

- id
- user_id
- folder_id
- source_url
- source_platform
- title
- custom_title
- author_name
- cover_image_url
- content_text
- raw_html_optional
- created_at

### note_snippets

存用户高亮选中的片段。

关键字段：

- id
- note_id
- user_id
- selected_text
- start_offset_optional
- end_offset_optional
- reason_text
- color_tag_id
- created_at

### color_tags

存用户自定义标签颜色和含义。

关键字段：

- id
- user_id
- name
- color_value
- description

### projects

存创作项目。

关键字段：

- id
- user_id
- name
- target_folder_id
- topic
- target_audience
- desired_tone
- desired_length
- status
- created_at

### analyses

存 AI 对参考库做出的分析结果。

关键字段：

- id
- project_id
- core_judgement
- effective_patterns
- user_preference
- reuse_suggestion
- wording_analysis_optional
- structure_analysis_optional
- reader_simulation_optional
- created_at

### drafts

存某一版文案。

关键字段：

- id
- project_id
- version_no
- full_text
- source_type
- parent_draft_id_optional
- created_at

### draft_blocks

存文案中的结构块。

关键字段：

- id
- draft_id
- block_key
- block_title
- block_order
- block_text
- block_color

### feedback_memories

存“去 AI 味”最关键的用户反馈记忆。

关键字段：

- id
- user_id
- project_id_optional
- draft_id_optional
- memory_type
- memory_text
- related_block_key_optional
- sentiment
- created_at

`memory_type` 建议枚举值：

- like
- dislike
- rewrite_preference
- ai_smell_feedback
- final_choice

---

## 11. 推荐技术栈

### 网页前端

你的朋友给的方向是对的，推荐采用：

- React
- Vite
- TypeScript
- shadcn/ui
- Tailwind CSS

建议补上：

- React Router：做多页面路由
- TanStack Query：管理接口请求和缓存
- Zustand：管理轻量全局状态
- React Hook Form：做表单
- Zod：做前端参数校验

### 插件前端

浏览器插件不建议直接手写原始 Chrome Extension 结构，推荐：

- WXT
- React
- TypeScript
- Manifest V3

原因：

- 更适合现代浏览器插件开发
- 和 Vite 生态兼容
- 比纯原生扩展脚手架更省心

### 后端

#### V1 首选方案

推荐：腾讯云 CloudBase

推荐原因：

- 面向中国大陆用户访问更稳
- 一个平台里可以同时拿到身份认证、数据库、云函数、文件存储、Web 托管
- 对 0 经验开发者更友好，能减少服务器运维压力
- 小体量产品前期不需要自己搭一整套服务器

V1 建议这样搭：

- 身份认证：CloudBase Auth
- 数据库存储：CloudBase 文档型数据库
- 后端接口：CloudBase 云函数，运行 Node.js
- 图片与封面：CloudBase 云存储
- 网页部署：CloudBase Web/静态托管

#### 为什么不建议你现在就上“传统后端”

比如：

- 自购云服务器
- 自己装 Nginx
- 自己配 Node 服务
- 自己配数据库和备份

这些不是不能做，而是对你当前阶段来说学习成本太高，容易把时间消耗在运维，而不是产品验证。

#### 未来升级路线

如果后续用户量变大、逻辑变复杂，可以再升级到：

- Node.js + NestJS
- MySQL / PostgreSQL
- CloudBase Run 或腾讯云轻量应用服务器 / CVM

但这不是现在第一阶段的重点。

### AI 接入层

建议不要把模型厂商写死在前端。

应该这样设计：

1. 前端只请求你自己的后端接口
2. 后端再去调用模型 API
3. API Key 只保存在后端环境变量里

这样做的原因：

- 更安全
- 后面换模型厂商更方便
- 方便做统一日志、限流、成本控制

---

## 12. AI 工作流建议

V1 建议拆成固定 5 步，而不是让一个大 prompt 包打天下。

### Step 1. 参考库预处理

输入：

- 笔记正文
- 标注片段
- 用户理由
- 颜色标签

输出：

- 结构化参考资料

### Step 2. 参考文案分析

默认输出短分析，字段建议固定：

- `core_judgement`：一句话判断，结论前置，直接说可复用写法
- `effective_patterns`：3 条可复用写法
- `user_preference`：从用户标注、标签、理由中提炼偏好
- `reuse_suggestion`：下一篇怎么套用

默认不输出长篇报告。

`core_judgement` 写作规则：

- 用“动词 + 结果/判断”的句式开头
- 删除“我发现”“这批文案值得学的是”“从整体来看”等自我说明
- 优先写用户下一篇能直接照着做的判断

如果用户追问，再补充：

- 用词风格细节
- 语气与表达偏好
- 结构模板总结
- 读者视角反馈

### Step 3. 方案与初稿生成

输出：

- 结构方案
- 分块草稿

### Step 4. 改稿协作

输出：

- 某一块的替代表达
- 调整后的连贯版本

### Step 5. 最终审稿

输出：

- AI 味风险点
- 读者视角行为反馈
- 可操作的修改建议

---

## 13. V1 的开发顺序建议

### Phase 0. 先把规划和数据结构定下来

目标：

- 明确页面
- 明确表结构
- 明确 AI 工作流

### Phase 1. 先做插件采集与保存

目标：

- 能抓到一篇笔记
- 能保存到文件夹
- 能标注片段

### Phase 2. 再做网页端项目创建与分析

目标：

- 能选择参考库
- 能跑出分析结果

### Phase 3. 再做初稿生成与结构编辑

目标：

- 能生成结构化草稿
- 能调整模块顺序

### Phase 4. 再做逐段改稿与版本记录

目标：

- 能保留每次修改
- 能对某一段单独讨论

### Phase 5. 最后做部署与上线准备

目标：

- 跑通正式环境
- 接入域名
- 补齐必要合规文案

---

## 14. 当前建议的取舍结论

### 保留

- 插件抓取
- 片段标注
- 文件夹参考库
- 分析页
- 结构化初稿
- 逐段协作改稿
- 版本记录
- 最终审稿

### 暂缓

- 真正意义上的模型训练
- 超复杂插件管理能力
- 团队系统
- 商业化支付
- 移动端深度适配

### 新增建议

- 增加 feedback_memories 作为“去 AI 味”的关键数据层
- 把 AI 功能拆成多步骤，而不是一个超级大 prompt
- 插件端和网页端共用同一套账号体系

---

## 15. 外部依赖与人工节点

请同步查看：

- [HUMAN_CHECKPOINTS.md](/Users/linshuo/Documents/New%20project/HUMAN_CHECKPOINTS.md)

那份文档会告诉你在开发到什么阶段时，需要你亲自注册账号、提供 API Key、购买域名、做备案、开通发布权限等。

---

## 16. 官方参考链接

以下链接用于支撑当前技术建议，后续如果云平台能力变动，以官方文档为准：

- 腾讯云 CloudBase 产品页：https://cloud.tencent.com/product/tcb
- 腾讯云 CloudBase Web 端 SDK：https://cloud.tencent.com/document/product/876/46332
- 腾讯云 CloudBase 云函数：https://cloud.tencent.com/document/product/876/44717
- 腾讯云 CloudBase 文档型数据库：https://cloud.tencent.com/document/product/876/125983
- 腾讯云 CloudBase 身份认证：https://cloud.tencent.com/document/product/876/46177
- 腾讯云 CloudBase 云存储：https://cloud.tencent.com/document/product/876/19372
- 腾讯云 CloudBase HTTP 访问服务：https://cloud.tencent.com/document/product/876/130728
- 腾讯云 CloudBase ICP 备案问题：https://cloud.tencent.com/document/product/876/128405
